import React from 'react';
import {Redirect, Route} from 'react-router-dom'

const PrivateRoute = ({}) => {
    return (
        <div>
            
        </div>
    );
};

export default PrivateRoute;